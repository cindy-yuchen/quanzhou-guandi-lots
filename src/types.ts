export interface UserForm {
  name: string;
  birthYear: string;
  city: string;
  category: string;
  question: string;
}

export interface Lot {
  id: number;
  poem: string;
  meaning: string;
  grade: string;
}
